// To calculate area of Square
# include <stdio.h>

int main(){
    int area;
    printf("Enter area of the Square:");
    scanf("%d", &area);
    area = area * area;
    printf("Square of the area is %d", area);
    return 0;
}